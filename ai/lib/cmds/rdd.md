Please read these directories in alphabetic order (and likewise for the files within):

```sh
├── /Users/zach/Documents/zv/projects/data/rdd/context
│   └── belief/
│   └── camino/
```

The files in `camino/` correspond to stages in our workflow, which we'll track using the `stage` key in our TOML frontmatter.

Let's get started.
