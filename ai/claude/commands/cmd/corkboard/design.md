Sort out design.

**If $ARGUMENTS is provided:** Read `/Users/zach/Documents/denv/dotfiles/agents/claude/commands/corkboard/find.md` and follow the task lookup steps with the query. Once found, display title and project briefly, then use the task body as context for the design session below.

**Design session:** Interview me until we reach a shared understanding. Walk down each branch of the design tree, resolving dependencies between decisions one-by-one. Ask the questions one at a time.
